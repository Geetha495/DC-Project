# You can add additional arguments such as number of clients, log file names etc
# Example: python file.py -n 3 
# For more arguments, refer the argparse related lines in the config.py file 

To run the peer-to-peer algorithm simulation:
    python p2p.py
To run the server-client algorithm simulation:
    python server.py

To compare the predictions on the test set:
    python evaluate.py
